version: "3.9"

services:
  sidequest:
    build: .
    container_name: sidequest
    ports:
      - "8788:8788"
    environment:
      NODE_ENV: production
      PORT: 8788
    restart: unless-stopped
